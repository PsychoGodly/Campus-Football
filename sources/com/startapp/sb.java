package com.startapp;

import android.webkit.WebView;
import java.lang.ref.WeakReference;

/* compiled from: Sta */
public class sb extends WeakReference<WebView> {
    public sb(WebView webView) {
        super(webView);
    }
}
